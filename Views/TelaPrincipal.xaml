<Window x:Class="Pim_04.Views.TelaPrincipal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="DeskPro - Painel Principal" Height="600" Width="1000"
        WindowState="Maximized"
        Background="#1E1E1E"
        ResizeMode="CanResize" MinWidth="800" MinHeight="500">
    <Window.Resources>
        <!-- Estilos (mesmos da versão anterior) -->
        <Style x:Key="SidebarButton" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#4A90E2"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Margin" Value="0,5"/>
            <Setter Property="Width" Value="130"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="#252526"/>
            <Setter Property="BorderBrush" Value="#3D3D3D"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="CornerRadius" Value="10"/>
        </Style>
        <Style x:Key="HeaderStyle" TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#4A90E2"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="5"/>
        </Style>
        <Style x:Key="CellStyle" TargetType="DataGridCell">
            <Setter Property="Background" Value="#2D2D2D"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#3D3D3D"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Sidebar ajustada com logo -->
            <Border Style="{StaticResource CardStyle}" Grid.Column="0" Margin="10,10,10,10">
                <StackPanel Background="#2D2D2D" Margin="-6,0,-6,0">
                    <TextBlock Text="Menu"
                               FontSize="20" FontWeight="Bold"
                               Foreground="White"
                               HorizontalAlignment="Center" Margin="0,10,0,20"/>
                    <Button x:Name="btnNovoChamado" Click="BtnNovoChamado_Click" Style="{StaticResource SidebarButton}" Width="130" Cursor="Hand">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Plus" Margin="5,0,3,0"/>
                            <TextBlock Text="Novo Chamado" Foreground="White"/>
                        </StackPanel>
                    </Button>
                    <Button x:Name="btnMeusChamados" Click="BtnMeusChamados_Click" Style="{StaticResource SidebarButton}" Width="130" Cursor="Hand">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="FormatListBulleted" Margin="5,0,3,0"/>
                            <TextBlock Text="Meus Chamados" Foreground="White"/>
                        </StackPanel>
                    </Button>
                    <Button x:Name="btnTodosChamados" Click="BtnTodosChamados_Click" Style="{StaticResource SidebarButton}" Width="130" Cursor="Hand" Visibility="Collapsed">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="ViewList" Margin="5,0,3,0"/>
                            <TextBlock Text="Todos Chamados" Foreground="White"/>
                        </StackPanel>
                    </Button>
                    <Button x:Name="btnRelatorios" Click="BtnRelatorios_Click" Style="{StaticResource SidebarButton}" Width="130" Cursor="Hand">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="ChartBar" Margin="5,0,3,0"/>
                            <TextBlock Text="Relatórios" Foreground="White"/>
                        </StackPanel>
                    </Button>
                    <Button x:Name="btnConfiguracoes" Click="BtnConfiguracoes_Click" Style="{StaticResource SidebarButton}" Width="130" Cursor="Hand">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Settings" Margin="5,0,3,0"/>
                            <TextBlock Text="Configurações" Foreground="White"/>
                        </StackPanel>
                    </Button>
                    <Button x:Name="btnSair" Click="BtnSair_Click" Background="Red" Margin="0,20,0,0" Width="130">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="ExitToApp" Margin="0,2,5,0"/>
                            <TextBlock Text="Sair" Foreground="White"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Border>

            <!-- Main Content -->
            <Border Style="{StaticResource CardStyle}" Grid.Column="1" Margin="10,10,20,20">
                <StackPanel>
                    <TextBlock Text="Bem-vindo ao DeskPro"
                               FontSize="28" FontWeight="SemiBold"
                               Foreground="#D4D4D4" Margin="40,10,0,20" Width="293"/>

                    <!-- Barra de Busca Estilizada com Ícone -->
                    <Grid Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox x:Name="txtBuscar" Width="300" Height="36"
                                 VerticalAlignment="Center"
                                 Background="#3D3D3D"
                                 Foreground="White"
                                 BorderBrush="#4A90E2"
                                 BorderThickness="1"
                                 Padding="10,0"
                                 KeyDown="TxtBuscar_KeyDown" Margin="219,0,219,0" UndoLimit="94" FontSize="20">
                            <TextBox.Style>
                                <Style TargetType="TextBox">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="TextBox">
                                                <Border Background="{TemplateBinding Background}"
                                                        BorderBrush="{TemplateBinding BorderBrush}"
                                                        BorderThickness="{TemplateBinding BorderThickness}"
                                                        CornerRadius="5">
                                                    <ScrollViewer x:Name="PART_ContentHost"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </TextBox.Style>
                        </TextBox>
                        <Button x:Name="btnBuscar" Click="BtnBuscar_Click"
                                Width="35" Height="35"
                                Margin="524,0,179,0"
                                Background="#4A90E2"
                                Foreground="White"
                                BorderThickness="0" Cursor="Hand">
                            <materialDesign:PackIcon Kind="Magnify" HorizontalAlignment="Center" VerticalAlignment="Center" />
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}"
                                                        CornerRadius="5">
                                                    <ContentPresenter/>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </Button.Style>
                        </Button>
                    </Grid>

                    <!-- DataGrid Estilizado -->
                    <DataGrid x:Name="dgChamados" AutoGenerateColumns="False"
                              Margin="0,0,0,20" CanUserAddRows="False" CanUserDeleteRows="False"
                              Background="#2D2D2D"
                              Foreground="White"
                              BorderBrush="#3D3D3D"
                              BorderThickness="1"
                              RowBackground="#252526"
                              AlternatingRowBackground="#2D2D2D"
                              ColumnHeaderStyle="{StaticResource HeaderStyle}"
                              CellStyle="{StaticResource CellStyle}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                            <DataGridTextColumn Header="Título" Binding="{Binding Titulo}" Width="*"/>
                            <DataGridTextColumn Header="Descrição" Binding="{Binding Descricao}" Width="2*"/>
                            <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Window>